{
  "name": "ability-settlement-optimizer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ability-settlement-optimizer/src",
  "projectType": "library",
  "targets": {
    "clean": {},
    "action:build": {
      "executor": "nx:run-commands",
      "dependsOn": ["policy-counter:build"],
      "options": {
        "commands": ["pnpm node ./scripts/bundle-vincent-ability/esbuild.js"],
        "cwd": "packages/ability-settlement-optimizer",
        "parallel": false
      }
    },
    "action:only:deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "{projectRoot}"
      }
    },
    "action:deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["action:build"],
      "options": {
        "commands": ["node scripts/deploy-lit-action.js"],
        "cwd": "packages/ability-settlement-optimizer"
      }
    },
    "build": {
      "executor": "@nx/js:tsc",
      "dependsOn": ["action:build"],
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/ability-settlement-optimizer/dist",
        "main": "packages/ability-settlement-optimizer/src/index.ts",
        "tsConfig": "packages/ability-settlement-optimizer/tsconfig.lib.json",
        "assets": [
          "packages/ability-settlement-optimizer/*.md",
          {
            "glob": "**/*",
            "input": "packages/ability-settlement-optimizer/src/generated",
            "output": "src/generated"
          }
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "{projectRoot}/jest.config.js",
        "passWithNoTests": true
      }
    }
  }
}
